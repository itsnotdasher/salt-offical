local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "KeySystem"
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

-- Main Frame with sleek black and glow effect
local MainFrame = Instance.new("Frame")
MainFrame.Name = "MainFrame"
MainFrame.Size = UDim2.new(0, 400, 0, 250)
MainFrame.Position = UDim2.new(0.5, -200, 0.5, -125)
MainFrame.AnchorPoint = Vector2.new(0.5, 0.5)
MainFrame.BackgroundColor3 = Color3.fromRGB(15, 15, 20)
MainFrame.BorderSizePixel = 0
MainFrame.ClipsDescendants = true

-- Glow effect
local Glow = Instance.new("ImageLabel")
Glow.Name = "Glow"
Glow.Size = UDim2.new(1, 40, 1, 40)
Glow.Position = UDim2.new(0, -20, 0, -20)
Glow.BackgroundTransparency = 1
Glow.Image = "rbxassetid://5028857084"
Glow.ImageColor3 = Color3.fromRGB(50, 100, 255)
Glow.ScaleType = Enum.ScaleType.Slice
Glow.SliceCenter = Rect.new(24, 24, 276, 276)
Glow.ImageTransparency = 0.8
Glow.Parent = MainFrame

local UICorner = Instance.new("UICorner")
UICorner.CornerRadius = UDim.new(0, 12)
UICorner.Parent = MainFrame

-- Top Bar with subtle gradient
local TopBar = Instance.new("Frame")
TopBar.Name = "TopBar"
TopBar.Size = UDim2.new(1, 0, 0, 40)
TopBar.BackgroundColor3 = Color3.fromRGB(25, 25, 35)
TopBar.BorderSizePixel = 0

local UICorner2 = Instance.new("UICorner")
UICorner2.CornerRadius = UDim.new(0, 12)
UICorner2.Parent = TopBar

local Title = Instance.new("TextLabel")
Title.Name = "Title"
Title.Size = UDim2.new(0.7, 0, 1, 0)
Title.Position = UDim2.new(0.15, 0, 0, 0)
Title.BackgroundTransparency = 1
Title.Text = "KEY SYSTEM"
Title.Font = Enum.Font.GothamBold
Title.TextColor3 = Color3.fromRGB(255, 255, 255)
Title.TextSize = 16
Title.TextTransparency = 0.1

local CloseButton = Instance.new("TextButton")
CloseButton.Name = "CloseButton"
CloseButton.Size = UDim2.new(0, 40, 0, 40)
CloseButton.Position = UDim2.new(1, -40, 0, 0)
CloseButton.BackgroundTransparency = 1
CloseButton.Text = "×"
CloseButton.Font = Enum.Font.GothamBold
CloseButton.TextColor3 = Color3.fromRGB(255, 255, 255)
CloseButton.TextSize = 24
CloseButton.TextTransparency = 0.3

-- Content area
local Content = Instance.new("Frame")
Content.Name = "Content"
Content.Size = UDim2.new(1, -40, 1, -80)
Content.Position = UDim2.new(0, 20, 0, 60)
Content.BackgroundTransparency = 1

-- Key Entry with modern design
local KeyEntry = Instance.new("TextBox")
KeyEntry.Name = "KeyEntry"
KeyEntry.Size = UDim2.new(1, 0, 0, 50)
KeyEntry.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
KeyEntry.BorderSizePixel = 0
KeyEntry.Font = Enum.Font.GothamMedium
KeyEntry.PlaceholderText = "Enter your key..."
KeyEntry.PlaceholderColor3 = Color3.fromRGB(150, 150, 150)
KeyEntry.Text = ""
KeyEntry.TextColor3 = Color3.fromRGB(255, 255, 255)
KeyEntry.TextSize = 14
KeyEntry.TextXAlignment = Enum.TextXAlignment.Center

local UICorner3 = Instance.new("UICorner")
UICorner3.CornerRadius = UDim.new(0, 8)
UICorner3.Parent = KeyEntry

-- Submit Button with glow effect
local SubmitButton = Instance.new("TextButton")
SubmitButton.Name = "SubmitButton"
SubmitButton.Size = UDim2.new(1, 0, 0, 45)
SubmitButton.Position = UDim2.new(0, 0, 0, 70)
SubmitButton.BackgroundColor3 = Color3.fromRGB(60, 110, 220)
SubmitButton.BorderSizePixel = 0
SubmitButton.Font = Enum.Font.GothamBold
SubmitButton.Text = "SUBMIT"
SubmitButton.TextColor3 = Color3.fromRGB(255, 255, 255)
SubmitButton.TextSize = 16

local SubmitGlow = Instance.new("ImageLabel")
SubmitGlow.Name = "SubmitGlow"
SubmitGlow.Size = UDim2.new(1, 20, 1, 20)
SubmitGlow.Position = UDim2.new(0, -10, 0, -10)
SubmitGlow.BackgroundTransparency = 1
SubmitGlow.Image = "rbxassetid://5028857084"
SubmitGlow.ImageColor3 = Color3.fromRGB(60, 110, 220)
SubmitGlow.ScaleType = Enum.ScaleType.Slice
SubmitGlow.SliceCenter = Rect.new(24, 24, 276, 276)
SubmitGlow.ImageTransparency = 0.9
SubmitGlow.ZIndex = -1
SubmitGlow.Parent = SubmitButton

local UICorner4 = Instance.new("UICorner")
UICorner4.CornerRadius = UDim.new(0, 8)
UICorner4.Parent = SubmitButton

-- Get Key Button with modern look
local GetKeyButton = Instance.new("TextButton")
GetKeyButton.Name = "GetKeyButton"
GetKeyButton.Size = UDim2.new(1, 0, 0, 30)
GetKeyButton.Position = UDim2.new(0, 0, 0, 130)
GetKeyButton.BackgroundTransparency = 1
GetKeyButton.Font = Enum.Font.GothamMedium
GetKeyButton.Text = "GET KEY"
GetKeyButton.TextColor3 = Color3.fromRGB(200, 200, 255)
GetKeyButton.TextSize = 12
GetKeyButton.TextTransparency = 0.2

-- Divider line
local Divider = Instance.new("Frame")
Divider.Name = "Divider"
Divider.Size = UDim2.new(1, 0, 0, 1)
Divider.Position = UDim2.new(0, 0, 0, 120)
Divider.BackgroundColor3 = Color3.fromRGB(60, 60, 80)
Divider.BorderSizePixel = 0
Divider.Parent = Content

-- Assemble the UI
TopBar.Parent = MainFrame
Title.Parent = TopBar
CloseButton.Parent = TopBar
Content.Parent = MainFrame
KeyEntry.Parent = Content
SubmitButton.Parent = Content
GetKeyButton.Parent = Content
Divider.Parent = Content
MainFrame.Parent = ScreenGui

-- Animation effects
local TweenService = game:GetService("TweenService")

local function ButtonEffect(button)
    local originalSize = button.Size
    local originalPos = button.Position
    local originalColor = button.BackgroundColor3
    local originalTextColor = button.TextColor3
    local originalTransparency = button.TextTransparency or 0
    
    button.MouseEnter:Connect(function()
        if button.BackgroundTransparency < 1 then
            TweenService:Create(button, TweenInfo.new(0.1), {
                BackgroundColor3 = Color3.fromRGB(80, 140, 240),
                TextColor3 = Color3.fromRGB(255, 255, 255)
            }):Play()
            TweenService:Create(button:FindFirstChild("SubmitGlow") or Instance.new("Frame"), TweenInfo.new(0.1), {
                ImageColor3 = Color3.fromRGB(80, 140, 240)
            }):Play()
        else
            TweenService:Create(button, TweenInfo.new(0.1), {
                TextColor3 = Color3.fromRGB(255, 255, 255),
                TextTransparency = 0
            }):Play()
        end
    end)
    
    button.MouseLeave:Connect(function()
        if button.BackgroundTransparency < 1 then
            TweenService:Create(button, TweenInfo.new(0.1), {
                BackgroundColor3 = originalColor,
                TextColor3 = Color3.fromRGB(255, 255, 255)
            }):Play()
            TweenService:Create(button:FindFirstChild("SubmitGlow") or Instance.new("Frame"), TweenInfo.new(0.1), {
                ImageColor3 = originalColor
            }):Play()
        else
            TweenService:Create(button, TweenInfo.new(0.1), {
                TextColor3 = originalTextColor,
                TextTransparency = originalTransparency
            }):Play()
        end
    end)
    
    button.MouseButton1Down:Connect(function()
        if button.BackgroundTransparency < 1 then
            TweenService:Create(button, TweenInfo.new(0.05), {
                Size = originalSize - UDim2.new(0, 5, 0, 5),
                BackgroundColor3 = Color3.fromRGB(40, 90, 200)
            }):Play()
        end
    end)
    
    button.MouseButton1Up:Connect(function()
        if button.BackgroundTransparency < 1 then
            TweenService:Create(button, TweenInfo.new(0.1), {
                Size = originalSize,
                BackgroundColor3 = Color3.fromRGB(60, 110, 220)
            }):Play()
        end
    end)
end

-- Apply effects to buttons
ButtonEffect(SubmitButton)
ButtonEffect(GetKeyButton)
ButtonEffect(CloseButton)

-- Functionality
GetKeyButton.MouseButton1Click:Connect(function()
    setclipboard("https://discord.gg/wHqn5eGCsU")
    
    local notification = Instance.new("Frame")
    notification.Name = "Notification"
    notification.Size = UDim2.new(1, -40, 0, 40)
    notification.Position = UDim2.new(0, 20, 1, -60)
    notification.BackgroundColor3 = Color3.fromRGB(30, 30, 40)
    notification.BorderSizePixel = 0
    
    local notifText = Instance.new("TextLabel")
    notifText.Size = UDim2.new(1, 0, 1, 0)
    notifText.BackgroundTransparency = 1
    notifText.Font = Enum.Font.GothamMedium
    notifText.Text = "Discord link copied to clipboard!"
    notifText.TextColor3 = Color3.fromRGB(200, 200, 255)
    notifText.TextSize = 12
    notifText.Parent = notification
    
    local UICorner5 = Instance.new("UICorner")
    UICorner5.CornerRadius = UDim.new(0, 8)
    UICorner5.Parent = notification
    
    notification.Visible = false
    notification.Parent = MainFrame
    
    notification.Visible = true
    TweenService:Create(notification, TweenInfo.new(0.2), {Position = UDim2.new(0, 20, 1, -100)}):Play()
    
    wait(3)
    
    TweenService:Create(notification, TweenInfo.new(0.2), {Position = UDim2.new(0, 20, 1, -60)}):Play()
    wait(0.2)
    notification:Destroy()
end)

SubmitButton.MouseButton1Click:Connect(function()
    if KeyEntry.Text == "saltyer" then
        -- Correct key
        TweenService:Create(MainFrame, TweenInfo.new(0.3), {Size = UDim2.new(0, 0, 0, 250)}):Play()
        wait(0.3)
        loadstring(game:HttpGet("https://raw.githubusercontent.com/itsnotdasher/Voluted-Hub-V-1.1/refs/heads/main/balls"))()
        ScreenGui:Destroy()
    else
        -- Incorrect key
        local originalColor = KeyEntry.BackgroundColor3
        TweenService:Create(KeyEntry, TweenInfo.new(0.1), {
            BackgroundColor3 = Color3.fromRGB(80, 40, 40),
            TextColor3 = Color3.fromRGB(255, 150, 150)
        }):Play()
        wait(0.5)
        TweenService:Create(KeyEntry, TweenInfo.new(0.3), {
            BackgroundColor3 = originalColor,
            TextColor3 = Color3.fromRGB(255, 255, 255)
        }):Play()
    end
end)

CloseButton.MouseButton1Click:Connect(function()
    TweenService:Create(MainFrame, TweenInfo.new(0.2), {Size = UDim2.new(0, 400, 0, 0)}):Play()
    wait(0.2)
    ScreenGui:Destroy()
end)

-- Initial animation
MainFrame.Size = UDim2.new(0, 0, 0, 0)
MainFrame.Visible = true
TweenService:Create(MainFrame, TweenInfo.new(0.3, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
    Size = UDim2.new(0, 400, 0, 250)
}):Play()

-- Add to player's GUI
local player = game.Players.LocalPlayer
ScreenGui.Parent = player:WaitForChild("PlayerGui")
